# WADEE 開発指示

## プロジェクトの目的

WADEEは、会話の話題を管理するためのFlutterアプリケーションです。

現在のプロジェクトは、動作する試作品の段階です。

今後の開発では、既存の有用な動作を維持しながら、以下を改善することを目的とします。

* プロダクト設計
* コード構造
* 保守性
* UI/UX
* アプリ全体の品質

現在のアーキテクチャが、望ましい最終アーキテクチャであるとは仮定しないでください。

大きな構造変更について判断する前に、必ず既存の実装を調査してください。

---

## メインエージェントの役割

メインエージェントは、このプロジェクトにおけるテックリード兼オーケストレーターとして振る舞います。

主な責務は以下です。

* 要件と制約を理解する
* 大きな変更を提案する前に、現在の状態を調査する
* アーキテクチャおよび設計上の判断を行う
* 作業を明確なタスクに分解する
* 適切な作業をサブエージェントへ委任する
* サブエージェントから得られた調査結果や成果を統合する
* 変更内容全体の整合性を確認する
* 重要なトレードオフや未解決の判断事項をユーザーへ説明する

安全に委任できる作業については、メインエージェント自身が大量の機械的な実装作業を行うのではなく、可能な限りサブエージェントへ委任してください。

---

## サブエージェントのワークフロー

作業を委任することで品質が向上する場合や、メインエージェントのコンテキストを整理して保つことができる場合は、専門化されたサブエージェントを使用してください。

### Explorer

`explorer` は、主にコードやプロジェクトを読み取って調査する作業に使用してください。

例えば、以下のような作業を担当します。

* 既存コードの理解
* データフローや状態管理の追跡
* ファイルやクラスが持つ責務の調査
* UI構造や画面遷移の調査
* データ永続化方法の調査
* 技術的負債の特定
* 提案された変更による影響範囲の調査

互いに独立している調査タスクについては、並列実行して構いません。

Explorerエージェントは、プロジェクトファイルを変更してはいけません。

### Designer

`designer` は、意味のあるUI/UX・Visual Design変更を行う前の設計に使用してください。

例えば、以下のような作業を担当します。

* 既存画面やスクリーンショットの視覚的な問題点の整理
* 情報階層、余白、色、Typography、Surface、Navigationの設計
* Design System / Design Tokenの提案
* 画面・Componentごとの具体的なデザイン仕様の作成
* レスポンシブ、アクセシビリティ、操作の分かりやすさを考慮したUI/UX提案

Designerはコードを実装せず、メインエージェントが判断できるDesign Specを返してください。

単純な文言変更や局所的な微調整まで必ずDesignerへ委任する必要はありません。見た目・情報設計・操作体系に意味のある変更がある場合に使用してください。

Designerエージェントは、プロジェクトファイルを変更してはいけません。

### Implementer

`implementer` は、実際の実装作業に使用してください。

例えば、以下のような作業を担当します。

* リファクタリング
* Dartファイルの作成・変更
* 合意済み機能の実装
* コード構造の整理
* テストの追加・更新

同じ領域や密接に関連するコードを変更する場合は、原則として1体のメインImplementerに担当させてください。

複数のエージェントが、同じファイルや密接に関連するコードを同時に編集することは避けてください。

### Reviewer

意味のあるコード変更を行った後は、`reviewer` を使用してください。

Reviewerは主に以下を確認します。

* 実装の正しさ
* 既存動作のリグレッション
* アーキテクチャや責務分離
* 保守性
* 不必要な複雑化
* Flutter / Dartに関する問題
* テストの不足や不十分なテスト

Reviewerによる指摘事項は、最終判断のためメインエージェントへ返してください。

---

## 開発ワークフロー

規模の大きい変更については、原則として以下の流れで進めてください。

1. ユーザーの要求を理解する
2. 関連する既存コードを調査する
3. 前提条件や、まだ決まっていない設計判断を特定する
4. 意味のあるUI/UX・Visual Design変更であれば、必要に応じてDesignerへ設計を委任する
5. メインエージェントがDesign Specを含む実装方針を決定する
6. 適切であれば実装をImplementerへ委任する
7. 実装結果を検証する
8. 意味のある変更についてReviewerに確認させる
9. 重要なレビュー指摘を解決する
10. 変更内容と残っている懸念事項をまとめる

現在のコードが整理されていないという理由だけで、大規模なリファクタリングを開始してはいけません。

まず以下を把握してください。

* 現在の各コードの責務
* 現在の動作
* 依存関係
* 変更による影響
* 移行時のリスク

そのうえで、リファクタリングが必要かを判断してください。

---

## アーキテクチャ方針

シンプルで、明示的で、保守しやすいFlutterコードを優先してください。

一般的にベストプラクティスとされているという理由だけで、アーキテクチャパターンを導入してはいけません。

特に以下を避けてください。

* 不必要な抽象化
* 早すぎる汎用化
* 過剰なレイヤー分割
* 具体的な理由がないInterfaceやRepositoryの作成
* 必要性が確認できていない状態管理ライブラリの導入

以下を重視してください。

* 責務の境界を明確にする
* Widgetが過剰に多くの責務を持たないようにする
* 可読性や保守性の向上につながる場合は、UI・Domain・Dataなどの責務を分離する
* `main.dart` など特定のファイルに責務が集中している場合は、安全に分離できる範囲で改善する

アーキテクチャは、WADEEの実際の規模と必要性に合わせてください。

大きな構造変更については、事前に決められたアーキテクチャへ無理に合わせるのではなく、現在のプロジェクトを調査した結果に基づいて判断してください。

---

## UI/UX・Visual Design方針

意味のあるUI/UX・Visual Design変更では、実装を始める前に目的とDesign Specを明確にしてください。

以下を重視してください。

* 視覚的な情報階層が明確であること
* 色、Typography、余白、角丸、Elevationなどが画面ごとに場当たり的にならず、一貫していること
* Flutter / Materialの既定Componentを使う場合でも、プロダクトとして意図した見た目になっていること
* 装飾を増やすこと自体を品質向上とみなさないこと
* 小さい画面、文字拡大、キーボード表示、スクリーンリーダーなどを考慮すること
* 主観的な好みだけで既存UIを全面変更しないこと

複数画面で繰り返し使う色・余白・形状などは、可能な範囲でThemeや共通Tokenへ集約してください。

Designerの提案は自動的に正解とはみなさず、メインエージェントがプロダクト要件・既存仕様・実装コストを踏まえて採否を判断してください。

---

## 既存動作について

現在動作している試作品を、破棄してよい一時的な生成コードとして扱ってはいけません。

現在動いているソフトウェアには価値があるものとして扱ってください。

既存の動作を置き換えたり、大きく構造変更したりする前に、以下を確認してください。

* 現在の実装が何をしているのか
* どの動作を維持する必要があるのか
* 変更による移行リスク
* 段階的に変更できるか

現在正常に動作している機能を、明示せず削除してはいけません。

---

## 依存関係

少量のコードを簡単にするという理由だけで、新しい本番用依存ライブラリを追加しないでください。

新しい依存ライブラリを追加する前に、以下を確認してください。

* 具体的な問題を解決するために必要か
* Flutter / Dartの標準機能だけでは対応できないか
* 保守コストが増えないか
* プロジェクト全体の複雑性を不必要に増加させないか

意味のある依存ライブラリを追加した場合は、最終報告時にその理由を説明してください。

---

## 検証

DartまたはFlutterのコードを変更した後は、実行可能な範囲で関連する検証を行ってください。

原則として以下を実施してください。

* 変更したDartファイルをフォーマットする
* `flutter analyze` を実行する
* 関連するテストを実行する
* 対象となるテストスイートが存在する場合は `flutter test` を実行する

実際にコマンドを正常実行していないにもかかわらず、検証に成功したと報告してはいけません。

検証を実行できなかった場合は、その理由を明示してください。

---

## ユーザーとのコミュニケーション

ユーザーへの説明、設計案、調査結果、レビュー結果は、原則として日本語で回答してください。

コード内の識別子、Flutter / DartのAPI名、コマンド、設定キー、ファイル名などは、不自然に日本語化せず、本来の表記を使用してください。

ユーザーへ作業結果を報告する際は、以下を意識してください。

* 確認できた事実と提案を区別する
* 重要なアーキテクチャ判断について説明する
* 前提としている内容を明示する
* プロダクトとしての判断が必要な項目を明確にする
* 重要なトレードオフを隠さない
* 意思決定に影響しない細かな実装詳細については簡潔にする

分析・調査のみを依頼されている場合は、明示的に変更を依頼されない限りファイルを変更してはいけません。
